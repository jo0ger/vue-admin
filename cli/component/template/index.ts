/**
 * @desc ${componentName}
 * @author ${author}
 * @date ${date}
 */

import Vue from '@/vue'
import { Component } from '@/utils/decorators'

@Component({
    name: '${componentName}',
})
export default class ${componentName} extends Vue {
}